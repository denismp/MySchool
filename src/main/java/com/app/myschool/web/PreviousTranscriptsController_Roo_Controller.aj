// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app.myschool.web;

import com.app.myschool.model.PreviousTranscripts;
import com.app.myschool.model.School;
import com.app.myschool.model.Student;
import com.app.myschool.web.PreviousTranscriptsController;
import java.io.UnsupportedEncodingException;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.joda.time.format.DateTimeFormat;
import org.springframework.context.i18n.LocaleContextHolder;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect PreviousTranscriptsController_Roo_Controller {
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String PreviousTranscriptsController.create(@Valid PreviousTranscripts previousTranscripts, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, previousTranscripts);
            return "previoustranscriptses/create";
        }
        uiModel.asMap().clear();
        previousTranscripts.persist();
        return "redirect:/previoustranscriptses/" + encodeUrlPathSegment(previousTranscripts.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String PreviousTranscriptsController.createForm(Model uiModel) {
        populateEditForm(uiModel, new PreviousTranscripts());
        return "previoustranscriptses/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String PreviousTranscriptsController.show(@PathVariable("id") Long id, Model uiModel) {
        addDateTimeFormatPatterns(uiModel);
        uiModel.addAttribute("previoustranscripts", PreviousTranscripts.findPreviousTranscripts(id));
        uiModel.addAttribute("itemId", id);
        return "previoustranscriptses/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String PreviousTranscriptsController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, @RequestParam(value = "sortFieldName", required = false) String sortFieldName, @RequestParam(value = "sortOrder", required = false) String sortOrder, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("previoustranscriptses", PreviousTranscripts.findPreviousTranscriptsEntries(firstResult, sizeNo, sortFieldName, sortOrder));
            float nrOfPages = (float) PreviousTranscripts.countPreviousTranscriptses() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("previoustranscriptses", PreviousTranscripts.findAllPreviousTranscriptses(sortFieldName, sortOrder));
        }
        addDateTimeFormatPatterns(uiModel);
        return "previoustranscriptses/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String PreviousTranscriptsController.update(@Valid PreviousTranscripts previousTranscripts, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, previousTranscripts);
            return "previoustranscriptses/update";
        }
        uiModel.asMap().clear();
        previousTranscripts.merge();
        return "redirect:/previoustranscriptses/" + encodeUrlPathSegment(previousTranscripts.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String PreviousTranscriptsController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        populateEditForm(uiModel, PreviousTranscripts.findPreviousTranscripts(id));
        return "previoustranscriptses/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String PreviousTranscriptsController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        PreviousTranscripts previousTranscripts = PreviousTranscripts.findPreviousTranscripts(id);
        previousTranscripts.remove();
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/previoustranscriptses";
    }
    
    void PreviousTranscriptsController.addDateTimeFormatPatterns(Model uiModel) {
        uiModel.addAttribute("previousTranscripts_lastupdated_date_format", DateTimeFormat.patternForStyle("M-", LocaleContextHolder.getLocale()));
        uiModel.addAttribute("previousTranscripts_createddate_date_format", DateTimeFormat.patternForStyle("M-", LocaleContextHolder.getLocale()));
    }
    
    void PreviousTranscriptsController.populateEditForm(Model uiModel, PreviousTranscripts previousTranscripts) {
        uiModel.addAttribute("previousTranscripts", previousTranscripts);
        addDateTimeFormatPatterns(uiModel);
        uiModel.addAttribute("schools", School.findAllSchools());
        uiModel.addAttribute("students", Student.findAllStudents());
    }
    
    String PreviousTranscriptsController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
